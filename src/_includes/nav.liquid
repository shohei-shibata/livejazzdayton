<nav>
  <div id="nav-container">
    <ul>
			{% for item in navItems %}
				<li>
					<a href={{item.url}}>
						<small>{{item.name}}</small>
					</a>
				</li>
			{% endfor %}
    </ul>
  </div>
</nav>
